<script setup lang="ts">
// Centered auth shell. The page sets the `*-page` body class via useHead
// (SSR-safe); AdminLTE's CSS centers the `.login-box` / `.register-box` itself.
</script>

<template>
  <slot />
</template>
